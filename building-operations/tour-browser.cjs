const {chromium}=require('playwright');const assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({channel:'chrome',headless:true,args:['--enable-webgl','--use-angle=swiftshader']});try{const p=await browser.newPage({viewport:{width:1450,height:1000}});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto(((process.env.ATLAS_TEST_URL||'http://localhost:59483')+'/building-operations/?example=house'));await p.locator('#viewport canvas').waitFor();await p.locator('#floor').selectOption('1');await p.locator('#startTour').click();assert.equal(await p.locator('#tourProgress').innerText(),'1 / 7');await p.locator('#tourNext').click();assert.equal(await p.locator('#tourTitle').innerText(),'펌프실과 급수 설비');await p.locator('#tourRecord').click();await p.locator('#atlasToolsForm [name=text]').fill('둘러보기에서 점검 기록 연결 확인');await p.locator('#atlasToolsForm button[type=submit]').click();await p.locator('#tourNext').click();assert(await p.locator('#isolateSystem').isChecked());await p.waitForTimeout(1200);await p.screenshot({path:'building-atlas-guided-tour.png'});await p.locator('#tourClose').click();assert.equal(await p.locator('#floor').inputValue(),'1');assert(!await p.locator('#isolateSystem').isChecked());await p.locator('#startTour').click();await p.locator('[data-sample=shop]').click();assert(await p.locator('#tourCard').isHidden());assert.deepEqual(errors,[]);console.log('PASS: tour navigation, record workflow, connected view, restore, building switch');}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1});
