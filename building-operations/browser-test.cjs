const {chromium}=require('playwright');
(async()=>{const browser=await chromium.launch({channel:'chrome',headless:true,args:['--enable-webgl','--use-angle=swiftshader']});const page=await browser.newPage({viewport:{width:1440,height:1000}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(((process.env.ATLAS_TEST_URL||'http://localhost:59483')+'/building-operations/'));await page.locator('#viewport canvas').waitFor();await page.waitForTimeout(1000);
await page.getByRole('button',{name:'＋ 기록 등록'}).click();await page.locator('[name=name]').fill('테스트 펌프');await page.locator('[name=source]').fill('현장 점검');await page.getByRole('button',{name:'저장하기'}).click();await page.getByRole('heading',{name:'테스트 펌프',exact:true}).waitFor();await page.reload();await page.locator('[data-record]').filter({hasText:'테스트 펌프'}).click();await page.getByRole('heading',{name:'테스트 펌프',exact:true}).waitFor();
await page.locator('#explode').check();await page.locator('#floor').selectOption('2');await page.getByRole('button',{name:'전체 보기',exact:true}).click();await page.locator('[data-layer=water]').uncheck();await page.locator('[data-layer=water]').check();await page.screenshot({path:'building-atlas-desktop.png'});await page.setViewportSize({width:390,height:844});await page.screenshot({path:'building-atlas-mobile.png',fullPage:true});if(errors.length)throw Error(errors.join('\n'));console.log('PASS: WebGL rendered; record saved and restored; filters and controls work; no page errors');await browser.close();})().catch(e=>{console.error(e);process.exit(1)});
