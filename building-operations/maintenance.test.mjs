import test from 'node:test';import assert from 'node:assert/strict';import {maintenanceSummary,validDate} from './maintenance.mjs';
test('dates and due boundaries',()=>{assert(!validDate('2026-02-30'));assert(validDate('2028-02-29'));const records=[{id:'a',category:'water',confidence:'현장 확인',status:'정상',nextInspection:'2026-09-12'},{id:'b',category:'electric',status:'점검 필요',nextInspection:'2026-09-13'},{id:'c',category:'lease'}];const s=maintenanceSummary({records},'2026-09-13');assert.equal(s.assets.length,2);assert.equal(s.unverified.length,1);assert.equal(s.overdue.length,1);assert.equal(s.dueToday.length,1);});
test('cost uses numeric entered values, tracks missing separately',()=>{const s=maintenanceSummary({records:[{category:'work',history:[{cost:12000},{cost:0},{cost:null},{cost:2000}]}]});assert.equal(s.recordedCost,14000);assert.equal(s.missingCost,1);assert.equal(s.historyCount,4);});
