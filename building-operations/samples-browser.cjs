const {chromium}=require('playwright');const assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({channel:'chrome',headless:true,args:['--enable-webgl','--use-angle=swiftshader']});try{const page=await browser.newPage({viewport:{width:1550,height:1080}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(((process.env.ATLAS_TEST_URL||'http://localhost:59483')+'/building-operations/?example=house'));await page.locator('#viewport canvas').waitFor();await page.waitForFunction(()=>document.querySelector('#title').textContent.includes('계단형'));assert.equal(await page.locator('#buildingSelect option').count(),2);await page.locator('#floor').selectOption('1');await page.waitForTimeout(1800);await page.screenshot({path:'building-atlas-sample-house.png'});await page.locator('[data-record="room-1-1"]').first().click();await page.getByRole('heading',{name:'101호',exact:true}).waitFor();await page.locator('#openFloorPlan').click();await page.locator('#planCanvas').waitFor();await page.screenshot({path:'building-atlas-sample-plan.png'});await page.locator('#planClose').click();await page.locator('[data-sample=shop]').click();await page.waitForFunction(()=>document.querySelector('#title').textContent.includes('근린상가'));assert.equal(await page.locator('#buildingSelect option').count(),3);await page.locator('[data-sample=house]').click();assert.equal(await page.locator('#buildingSelect option').count(),3);await page.reload();assert((await page.locator('#title').innerText()).includes('계단형'));await page.setViewportSize({width:390,height:844});assert(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));assert.deepEqual(errors,[]);console.log('PASS: templates added separately, 3D rooms, floor plan, no duplicates, saved selection, mobile');}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1});
